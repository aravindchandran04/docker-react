language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t aravindchandran/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true aravindchandran/docker-react npm run test
